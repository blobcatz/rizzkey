extends ./base

block meta
	//- Should be removed by the page when it loads, so that it won't needlessly
	//- stay when user navigates away via the navigation bar
	//- XXX: Remove navigation bar in auth page?
	meta(name='rizzkey:oauth:transaction-id' content=transactionId)
	meta(name='rizzkey:oauth:client-name' content=clientName)
	meta(name='rizzkey:oauth:scope' content=scope)
